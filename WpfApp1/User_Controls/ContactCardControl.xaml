<UserControl x:Class="WpfApp1.User_Controls.ContactCardControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.User_Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="350">

    <Border BorderBrush="LightGray" BorderThickness="1.5" CornerRadius="8" 
            Background="#F9F9F9" Padding="10" Margin="5" FlowDirection="RightToLeft">

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" Foreground="#333333" />
                <TextBlock Text="{Binding City, StringFormat='מגורים: {0}'}" FontSize="12" Foreground="Gray" />
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button Content="👁️" Width="35" Height="30" Margin="5,0" Click="DetailsButton_Click" ToolTip="הצג פרטים" />
                <Button Content="🗑️" Width="35" Height="30" Background="#FFEAEA" Foreground="Red" Click="DeleteButton_Click" ToolTip="מחק איש קשר" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>